import React from 'react';

import {
    DetailList,
} from './detailListStyle';

export default ({ data }) => {
    console.log(data);
    return (
        <DetailList>
            text
        </DetailList>
    )
};
